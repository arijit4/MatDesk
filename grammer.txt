factor  : INT|FLOAT
        : (PLUS|MINUS) factor
        : LPAREN expr RPAREN

power   : factor POW factor

term    : power ((MUL|DIV) power)*

expr    : (term (PLUS|MINUS) term)*
        : IDENTIFIER EQ expr